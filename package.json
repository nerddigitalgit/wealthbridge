{
  "name": "wealthbridge",
  "version": "1.0.0",
  "scripts": {
    "dev": "tinacms dev -c \"npx http-server . -p 3000\"",
    "build": "tinacms build --skip-cloud-checks || tinacms build --local || echo 'Using static admin'"
  },
  "devDependencies": {
    "@tinacms/cli": "^1.6.7",
    "tinacms": "^2.2.7"
  }
}
