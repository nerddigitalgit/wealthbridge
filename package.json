{
  "name": "wealthbridge",
  "version": "1.0.0",
  "scripts": {
    "build": "tinacms build --skip-cloud-checks"
  },
  "devDependencies": {
    "@tinacms/cli": "^1.10.0",
    "tinacms": "^2.8.0"
  }
}
